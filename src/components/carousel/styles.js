import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop: 5,
    },
    image: {
        width: '100%',
        aspectRatio: 3 / 3,
        resizeMode: 'cover',
        borderWidth: 1,
        top: 10,
        //zIndex: 100,
    },
});

export default styles;
